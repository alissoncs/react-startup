import React from 'react'
import ReactDOM from 'react-dom'
import SelfManage from './components/SelfManage'

const node = document.getElementById('application')

ReactDOM.render(<SelfManage />, node)
